# v0.3 Validation Ledger

Date: 2026-07-16

This ledger records release-gate evidence for the v0.3 streaming reliability
goal. It contains no destinations, stream keys, credentials, or captured media.

## Automated Gates

- `swift test`: 351 tests passed.
- `MAC_STREAM_ENABLE_HAISHINKIT=1 swift test`: 351 tests passed.
- `swift build -c release --arch arm64`: passed.
- `MAC_STREAM_ENABLE_HAISHINKIT=1 swift build -c release --arch arm64`: passed.
- `MAC_STREAM_ENABLE_MLX=1 swift build -c release --arch arm64`: passed.
- `MAC_STREAM_ENABLE_HAISHINKIT=1 MAC_STREAM_ENABLE_MLX=1 swift build -c release --arch arm64`: passed.

The test suites include runtime RTMP recovery, record-while-streaming ownership,
A/V timing, bounded append queues, adaptive backpressure response, secret
redaction, lifecycle cancellation, and backward-compatible session reports.

## Packaging Gates

- Packaged an arm64 release app with HaishinKit enabled, version `0.3.0`, and an
  isolated development bundle identifier.
- Verified the app and embedded Sparkle components with `codesign --verify`.
- Created a local DMG, mounted it read-only, confirmed `MacStream.app` and the
  `/Applications` link, detached it, and passed `hdiutil verify`.
- Release automation now signs the DMG with Developer ID Application, submits it
  with `notarytool`, staples it, validates it with Gatekeeper, and creates the
  checksum after stapling.

The signed/notarized DMG path still requires one successful GitHub Actions release
run before the gate is closed.

## Packaged UI QA

- Launched the isolated packaged app through CuaDriver without taking focus.
- Confirmed no startup Keychain or AI-provider prompt appeared.
- Confirmed BRB leaves camera and screen capture idle.
- Confirmed Screen + Webcam selection changes the preview and exposes explicit
  first-run Camera and Screen Recording recovery actions.
- Confirmed Live, Layout, Sources, and Health inspector navigation.
- Confirmed A/V drift, maximum drift, RTMP queue, audio rejection, recovery, and
  resource metrics render without overlap.
- Fixed and tested first-run permission copy that previously rendered
  "permission is ask."
- Removed the isolated QA app after validation.

## Idle Performance Baseline

Scenario: packaged HaishinKit release build, BRB selected, no active capture.

Sampling: 11 samples over 10 seconds at one-second intervals.

| Metric | Result |
| --- | --- |
| Average CPU | 0.00% |
| Maximum CPU | 0.00% |
| Average RSS | 151.8 MB |
| Maximum RSS | 151.8 MB |
| Thread count | 8 |

This proves the app can remain lightweight while open and capture-idle. It does
not replace active Screen + Webcam streaming measurements.

## Required Before Tagging v0.3.0

- Grant fresh Camera, Microphone, and Screen Recording permissions to the exact
  release candidate and verify recovery after relaunch.
- Run 60 minutes of real Screen + Webcam RTMP publishing against a private ingest.
- Run 60 minutes of real RTMP publishing with record-while-streaming enabled.
- Capture CPU, RSS, thread count, dropped frames, queue depth, recovery metrics,
  and A/V drift for both long sessions.
- Interrupt the network for approximately 5 seconds and 30 seconds; verify
  bounded recovery, truthful state, and recording ownership.
- Run 20 packaged start/stop lifecycle cycles without stale capture, writer, or
  publisher state.
- Run the signed release workflow and verify the notarized/stapled DMG and Sparkle
  ZIP from GitHub Releases on a clean macOS user or machine.

Do not create or push the v0.3.0 tag until these remaining gates are recorded as
passed or the release owner explicitly changes the acceptance criteria.
